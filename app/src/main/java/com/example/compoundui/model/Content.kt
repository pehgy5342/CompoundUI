package com.example.compoundui.model

class Content {
    data class Label1(val tag: String)

    data class Label2(val title: String, val id: String, val name: String)

    data class Rv(val title: String,val name: String,)
}